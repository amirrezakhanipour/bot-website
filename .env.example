# Public Supabase values (safe for browser use when RLS is correctly configured)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only secrets are intentionally not named with NEXT_PUBLIC_.
# Add provider-specific/server-only variables only when the relevant phase is implemented.
# Never commit real secret values.
